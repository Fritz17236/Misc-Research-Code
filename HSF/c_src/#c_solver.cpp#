/*
 * c_solver.cpp
 * A numerical solver for use in HSF Simulations in Python
 * Chris Fritz, 3/2020
 * Github: Fritz17236
 */

#include "c_solver.h"
 

void run_sim(int *num) {
  add_one(num);
  //setup data
  //run loop
  // add data back to object
  return;void add_one(int *num) {
  std::cout << "ADD ONE CALLED" << std::endl;
  std::cout << *num << " ---> " << ++(*num) << std::endl;
  return;
}

